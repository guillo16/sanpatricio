<%= render 'shared/navbar' %>
<div class="container pt-5 mt-5 bg-white p-5 pb-5 mb-5">
  <h2 class="font-weight-bold text-center">Crear Usuario</h2>
  <%= simple_form_for @user do |f| %>
  <%= f.input :email %>
  <p class="h6 font-weight-bold text-secondary">La contraseña del usuario sera:  sanpatricio</p>
  <%= f.submit 'Crear usuario', class: "btn btn-primary btn-block mb-3" %>
  <div class="text-right pt-3">
    <%= link_to 'Volver', user_path(current_user), class: "btn btn-secondary" %>
  </div>
  <% end %>
</div>
<br><br><br><br><br><br><br><br>
